import Layout from '@/layouts/Layout';

import type { NextPage } from 'next';

const Run: NextPage = () => {
    return (
        <Layout title="実行">
            <p>実行ページ</p>
        </Layout>
    );
};

export default Run;
